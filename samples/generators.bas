VAR arr =  0 TO 10 STEP 2
; DIM arr as integer[6] = [0, 2, 4, 6, 8, 10]

VAR alphabet = `A` TO `Z`
; DIM alphabet as char[27] = ['A', 'B', 'C', ..., 'Z']

FOR VAR i IN 1 TO 10
NEXT
; FOR VAR i = 1 TO 10
; NEXT